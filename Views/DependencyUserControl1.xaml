<UserControl x:Class="WPFPages.DependencyUserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFPages.UserControls"
	   	 xmlns:views="clr-namespace:WPFPages"

             mc:Ignorable="d" 
             d:DesignHeight="25" d:DesignWidth="200">
	<UserControl.Resources>
		<Style x:Key="Userstyle" TargetType="TextBlock">
			<Setter Property="Width" Value="200" />
			<Setter Property="Height" Value="25" />
			<Setter Property="HorizontalAlignment" Value="Center" />
			<Setter Property="Background" Value="Lightgreen" />
		</Style>
		
	</UserControl.Resources>

	<Grid Height="25" Width="200">

		<Button>
			
		</Button>
		<TextBlock x:Name="tbTest"
				 HorizontalAlignment="Center"
				 Style="{StaticResource Userstyle}">
		

		</TextBlock>
    </Grid>
</UserControl>
